# MRNA Mainline Cleanup

Archived on 2026-07-18 while the fixed-manifest Attention training line was active.

`tracked_archive/` contains superseded capacity probes, the retired mixed curriculum
case, a legacy README training wrapper, and migration-package copies already replaced
by the active `TD3/` and `catkin_ws/` implementations. None are training entry points.
